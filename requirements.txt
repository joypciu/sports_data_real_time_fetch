# Core data fetch & enrichment
httpx
curl_cffi          # Cloudflare-bypass HTTP client (used by soccer_sofascore_props)

# Database
duckdb
pandas

# Stats API service (stats_api.py)
fastapi
uvicorn[standard]
python-dotenv

# Testing
pytest
pytest-asyncio
